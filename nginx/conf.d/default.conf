# Default server - return 444 for unknown Host (e.g. request to bare IP)
server {
    listen 80 default_server;
    server_name _;
    return 444;
}
server {
    listen 443 ssl default_server;
    server_name _;
    include /etc/nginx/conf.d/ssl-common.conf;
    return 444;
}
